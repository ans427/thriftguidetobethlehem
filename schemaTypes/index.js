import { thriftStoreType } from "./thriftStoreType";
import { resourceArticleType } from "./resourceArticleType";
import { storeCommentType } from "./storeCommentType";

export const schemaTypes = [thriftStoreType, resourceArticleType, storeCommentType];
